// forif loop on object

const myObject = {
    js: "javascript",
    cpp: "c++",
    rb: "ruby",
    swift: "swift by apple"
}

// for (const key in myObject) {
//     console.log(`${key} is shortcut for ${myObject[key]}`);
// }

for (const key in myObject) {
    //console.log(`${key} is shortcut for ${myObject[key]}`);
    }


    const programing=["js", "css", "react", "node", "next", "redux"]

    for (const key in programing) {
       //console.log(programing);
        }
   // for (const key in programing) {
        //console.log(key);
    // }
       
    // for (const key in programing) {
    //     console.log(programing[key]);
    // }

   //iteration on map is not possible either in forin function also
   
   